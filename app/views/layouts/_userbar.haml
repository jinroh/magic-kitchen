#user_bar
  - if user_signed_in?
    %span Signed in as #{link_to(current_user.name, user_root_path)}
    %br
    %span= link_to("Sign out", destroy_user_session_path)
  - else
    %span= link_to("Join", new_user_registration_path) + " or " + link_to("Log in", new_user_session_path)
